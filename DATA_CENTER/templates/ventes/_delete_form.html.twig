<form method="post" action="{{ path('app_ventes_delete', {'id': vente.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vente.id) }}">
    <button class="btn">Delete</button>
</form>
